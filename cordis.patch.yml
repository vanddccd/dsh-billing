# dsh-billing 组合包 patch 层：自激活（安装后无需手改 profile 配置）。
# 定价等全部配置可选，默认值见 host.js 顶部（含 2026-08-17 起峰谷价自动切换）。
# 注：baseURL 不在此配置——host.js 内部已做 DEEPSEEK_BASE_URL 环境变量 fallback。
- insert:
    - id: deepseek-billing
      name: dsh-billing
      config:
        apiKeyEnv: DEEPSEEK_API_KEY
